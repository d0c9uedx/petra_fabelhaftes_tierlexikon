# Kopieren nach .env und anpassen (Docker-Compose-Host-Port-Bindung, siehe docker-compose.yml)
# Für lokale Entwicklung nicht nötig – Defaults entsprechen bisherigem Verhalten
# (alle Interfaces, Port 8000).
HOST_BIND=0.0.0.0
HOST_PORT=8000
